/**
 * Real Project Management Dashboard
 * No fake data - uses actual GraphQL API (3004), Validation API (3005), and Quality Pipeline
 */

import React from 'react';
import { RealWorkflowDashboard } from './RealWorkflowDashboard';

const WorkflowDashboard: React.FC = () => {
  return <RealWorkflowDashboard />;
};

export default WorkflowDashboard;